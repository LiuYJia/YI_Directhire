import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RecloginPage } from './reclogin';

@NgModule({
  declarations: [
    RecloginPage,
  ],
  imports: [
    IonicPageModule.forChild(RecloginPage),
  ],
})
export class RecloginPageModule {}
